# resume-topic

This folder contains the OpenClaw skill implementation.

- `SKILL.md` is the skill definition used by OpenClaw

You usually don’t need anything else besides copying this folder into `~/.openclaw/skills/`.
